#include <stdio.h>
/**
 * main - Entry point
 * Des: 'prints all the numbers of base 16 in lowercase'
 * Return: Always 0
 */
int main(void)
{
	int n;
	int m;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	for (n = 97; m < 103; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
